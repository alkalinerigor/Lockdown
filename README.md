# Lockdown
Lockdown Lab on CyberDefenders simulates a full-spectrum targeted intrusion on a Windows IIS server. An attacker uses Nmap for discovery, exploits SMB credentials via Impacket to enumerate shares (IPC$), and uploads a malicious web shell (shell.aspx) to spawn a reverse shell, ultimately deploying AgentTesla malware for persistence.
